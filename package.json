{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run --parallel --stream dev",
    "dev": "lerna run --parallel --stream dev"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
