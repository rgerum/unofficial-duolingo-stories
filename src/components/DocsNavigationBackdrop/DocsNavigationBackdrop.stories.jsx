import DocsNavigationBackdrop from "./DocsNavigationBackdrop";

const meta = {
  component: DocsNavigationBackdrop,
  argTypes: {
  },
};

export default meta;

export const Normal = {
  render: (args) => (
    <DocsNavigationBackdrop {...args}>
    </DocsNavigationBackdrop>
  ),
};
