import styles from "./fade_glide_in.module.css"

export default function FadeGlideIn({children}) {
    return <div <div className={styles.fadeGlideIn+" "+hidden2} onClick={onClick} data-lineno={element?.editor?.block_start_no}>>{children}</div>
}